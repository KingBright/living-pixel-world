# Living Pixel World

Rust 核心的俯视角像素开放世界 RPG。

## 发布状态

**完整 0.8 源码尚未发布到 `main`。当前主分支只保存初始化文件、工作约定和上传状态，不能构建游戏。**

2026-09-08，按照仓库所有者要求，后续迭代改为直接提交 `main`，不新建功能分支或 PR。

本次真实 GitHub 写入已经成功到达部分文件，但完整上传在生态模块被平台以“无法确定请求的安全状态”拦截；相同请求重试一次仍被拦截。该结果不是 GitHub 返回的 403 写入权限拒绝。没有改写、重编码或使用其他路径上传被拦截的源码。

最新完整交付为聊天附件中的 Living Pixel World 0.8。184 个原始文件中，61 个已经写入 GitHub 的待提交文件树；这个不完整文件树没有接到 `main`，以免把它误当成完整工程。详见 [上传状态](IMPORT_STATUS.md)。

源码交付与编译、测试、窗口及性能验收是不同状态。本次发布操作没有执行这些验收，也没有宣称测试通过。
